# Documentación de despliegue

Describe los entornos admitidos, los artefactos publicados, las variables requeridas, el orden de despliegue, las verificaciones posteriores y el procedimiento de reversión.

Las instrucciones deben poder repetirse sin depender de rutas, cuentas o valores exclusivos de una computadora.
